# Define a function that computes the length of a given list or string.

def str_len(str):
    str = "i study in SYBCA"
    print("the length of string is", len(str))

def list_len(list):
    list = ("uday", "ronit", "shravani", "aryan")
    print("the length of list is", len(list))

str_len(str)
list_len(list)
